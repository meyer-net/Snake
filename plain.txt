网关后期可改善：
01：plugins分组路由实验，依据业务做特性组合模式(参照鉴权模式拆分业务上下文)。
02：剥离规则匹配区域，作为独立配置式附加至plugin。
03：收纳参数验证模式，通过特定的url加入正则匹配等表达式验证。
04：新增对下游业务rpc的支持，例如thrift，grpc。
05：新增服务注册发现体系，运用shared_dict，consul等。
06：新增参数格式上下文路由转换，类似聚合，将api数据做特定分解并在上下游支持resful风格配置。
07：将buffer功能提取为插件化功能，并纳入分组区域。
08：日志部分重构，并采用写入elk的设定。
09：独立化网关为命令台项目，通过常规py进行打包安装或用docker。
10：下游服务upstrem增设反压力敏感与动态弹性负载ELB，通过抽象配置化与响应演算请求服务器及动态的灰度a/b支持。
11：资源静态化类型提取，浏览器识别抽样成字典，并在项目初始化脚本时完成初始数据的生成。
12：请求统计响应数据收入单独的存储器，以0.01s为单位异步入缓存(单条)，并对4xx,5xx数据进行详细记载，写入elk/下游数据分析流进行分析。
13：完成lvs下网关集群配置共享，可配合5中consul服务注册发现，完成最新配置同步作业。
14：新增代理请求插件，处理当前服务器接受的请求参数，并在当前服务器做二次请求处理(还原原始请求)。类似业务系统提交聚合拿到特定的商城信息，并在商城的提交处做隐秘提交，并切换url。
15：国际链路支持插件，解决访问下游服务商遇到网络瓶颈的情况，可配置多通道进行弹性的自动切换(vpn，代理，dns等多模式)，并下发重启指令，单个timer做1s/次心跳检测链路，并及时做恢复切换。
16：调用链跟踪，通过创建上下文唯一标识ID，确定单次请求全过程的上下文调用链，方便捕获、追踪定位具体某服务环节的问题。
17：根据不同的请求（二级域名/顶层文件夹），适配本地不同的alias